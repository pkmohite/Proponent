runtime: python
env: flex

runtime_config:
  python_version: 3

entrypoint: streamlit run Home.py --server.port $PORT

automatic_scaling:
  max_num_instances: 1

# Or manual scaling as below:
# manual_scaling:
#   instances: 1